# Date: 2026-06-10
## Topic
Autocomplete (TypeScript + Trie)

## What I Did
- Dived deep into Tries and understood their implementation.
- Refreshed my knowledge of Big O analysis

## Problems / Challenges
The algorithm is clear overall, but the tests were a bit confusing
because they look for a specific word order in the output.

## Solutions / Attempts
Fixing the issue mentioned above resolved 2 tests at once
(word order in the output and performance). The solution was to build the Trie using normalized,
case-insensitive words (e.g., 'APPLE' and 'apple' merge into a single branch instead of two separate
ones). The only trade-off is that we have to filter the original array at the end
to retrieve the words in their original casing, but this approach keeps the tree
significantly smaller.

## Time Spent
~ 5 hours
